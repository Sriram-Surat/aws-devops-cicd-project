pipeline {
    agent any

    stages {

        stage('Terraform Init') {
            steps {
                dir('terraform') {
                    bat 'terraform init'
                }
            }
        }

        stage('Terraform Destroy') {
            steps {
                dir('terraform') {
                    bat 'terraform destroy -auto-approve'
                }
            }
        }

    }
}